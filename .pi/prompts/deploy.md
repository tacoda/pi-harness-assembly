Load the `deploy-runbook` skill and follow it exactly.

Ask me for the service, environment, and change summary if I haven't given
them. Then run the pipeline stage by stage using the `deploy_service` tool.
Stop before promoting to prod.
